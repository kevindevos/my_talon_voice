mode: dictation
language: nl
-
opdrachtmodus:
    mode.disable("dictation")
    mode.disable("user.dutch")
    mode.enable("command")

opdracht modus:
    mode.disable("dictation")
    mode.disable("user.dutch")
    mode.enable("command")

opdrachten modus:
    mode.disable("dictation")
    mode.disable("user.dutch")
    mode.enable("command")

<phrase>: insert("{phrase} ")
