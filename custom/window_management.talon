weber [<phrase>]$:
    user.switcher_focus("Chrome")
    user.parse_phrase(phrase or "")
web [<phrase>]$:
    user.switcher_focus("Chrome")
    user.parse_phrase(phrase or "")
coder [<phrase>]$:
    user.switcher_focus("Code")
    user.parse_phrase(phrase or "")
code [<phrase>]$:
    user.switcher_focus("Code")
    user.parse_phrase(phrase or "")